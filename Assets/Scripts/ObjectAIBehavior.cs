using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectAIBehavior : MonoBehaviour
{
    public bool canSoundPassThrough;
    public bool canLightPassThrough;
    public bool canActorsPassThrough;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
